// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"cambridge-hit.com/gin-base/activateserver/app/entity/ent/metricevent"
	"cambridge-hit.com/gin-base/activateserver/app/entity/ent/predicate"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MetricEventQuery is the builder for querying MetricEvent entities.
type MetricEventQuery struct {
	config
	ctx        *QueryContext
	order      []metricevent.OrderOption
	inters     []Interceptor
	predicates []predicate.MetricEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MetricEventQuery builder.
func (meq *MetricEventQuery) Where(ps ...predicate.MetricEvent) *MetricEventQuery {
	meq.predicates = append(meq.predicates, ps...)
	return meq
}

// Limit the number of records to be returned by this query.
func (meq *MetricEventQuery) Limit(limit int) *MetricEventQuery {
	meq.ctx.Limit = &limit
	return meq
}

// Offset to start from.
func (meq *MetricEventQuery) Offset(offset int) *MetricEventQuery {
	meq.ctx.Offset = &offset
	return meq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (meq *MetricEventQuery) Unique(unique bool) *MetricEventQuery {
	meq.ctx.Unique = &unique
	return meq
}

// Order specifies how the records should be ordered.
func (meq *MetricEventQuery) Order(o ...metricevent.OrderOption) *MetricEventQuery {
	meq.order = append(meq.order, o...)
	return meq
}

// First returns the first MetricEvent entity from the query.
// Returns a *NotFoundError when no MetricEvent was found.
func (meq *MetricEventQuery) First(ctx context.Context) (*MetricEvent, error) {
	nodes, err := meq.Limit(1).All(setContextOp(ctx, meq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{metricevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (meq *MetricEventQuery) FirstX(ctx context.Context) *MetricEvent {
	node, err := meq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MetricEvent ID from the query.
// Returns a *NotFoundError when no MetricEvent ID was found.
func (meq *MetricEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = meq.Limit(1).IDs(setContextOp(ctx, meq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{metricevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (meq *MetricEventQuery) FirstIDX(ctx context.Context) int {
	id, err := meq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MetricEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MetricEvent entity is found.
// Returns a *NotFoundError when no MetricEvent entities are found.
func (meq *MetricEventQuery) Only(ctx context.Context) (*MetricEvent, error) {
	nodes, err := meq.Limit(2).All(setContextOp(ctx, meq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{metricevent.Label}
	default:
		return nil, &NotSingularError{metricevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (meq *MetricEventQuery) OnlyX(ctx context.Context) *MetricEvent {
	node, err := meq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MetricEvent ID in the query.
// Returns a *NotSingularError when more than one MetricEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (meq *MetricEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = meq.Limit(2).IDs(setContextOp(ctx, meq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{metricevent.Label}
	default:
		err = &NotSingularError{metricevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (meq *MetricEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := meq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MetricEvents.
func (meq *MetricEventQuery) All(ctx context.Context) ([]*MetricEvent, error) {
	ctx = setContextOp(ctx, meq.ctx, "All")
	if err := meq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MetricEvent, *MetricEventQuery]()
	return withInterceptors[[]*MetricEvent](ctx, meq, qr, meq.inters)
}

// AllX is like All, but panics if an error occurs.
func (meq *MetricEventQuery) AllX(ctx context.Context) []*MetricEvent {
	nodes, err := meq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MetricEvent IDs.
func (meq *MetricEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if meq.ctx.Unique == nil && meq.path != nil {
		meq.Unique(true)
	}
	ctx = setContextOp(ctx, meq.ctx, "IDs")
	if err = meq.Select(metricevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (meq *MetricEventQuery) IDsX(ctx context.Context) []int {
	ids, err := meq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (meq *MetricEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, meq.ctx, "Count")
	if err := meq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, meq, querierCount[*MetricEventQuery](), meq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (meq *MetricEventQuery) CountX(ctx context.Context) int {
	count, err := meq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (meq *MetricEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, meq.ctx, "Exist")
	switch _, err := meq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (meq *MetricEventQuery) ExistX(ctx context.Context) bool {
	exist, err := meq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MetricEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (meq *MetricEventQuery) Clone() *MetricEventQuery {
	if meq == nil {
		return nil
	}
	return &MetricEventQuery{
		config:     meq.config,
		ctx:        meq.ctx.Clone(),
		order:      append([]metricevent.OrderOption{}, meq.order...),
		inters:     append([]Interceptor{}, meq.inters...),
		predicates: append([]predicate.MetricEvent{}, meq.predicates...),
		// clone intermediate query.
		sql:  meq.sql.Clone(),
		path: meq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Type string `json:"type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MetricEvent.Query().
//		GroupBy(metricevent.FieldType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (meq *MetricEventQuery) GroupBy(field string, fields ...string) *MetricEventGroupBy {
	meq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MetricEventGroupBy{build: meq}
	grbuild.flds = &meq.ctx.Fields
	grbuild.label = metricevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Type string `json:"type,omitempty"`
//	}
//
//	client.MetricEvent.Query().
//		Select(metricevent.FieldType).
//		Scan(ctx, &v)
func (meq *MetricEventQuery) Select(fields ...string) *MetricEventSelect {
	meq.ctx.Fields = append(meq.ctx.Fields, fields...)
	sbuild := &MetricEventSelect{MetricEventQuery: meq}
	sbuild.label = metricevent.Label
	sbuild.flds, sbuild.scan = &meq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MetricEventSelect configured with the given aggregations.
func (meq *MetricEventQuery) Aggregate(fns ...AggregateFunc) *MetricEventSelect {
	return meq.Select().Aggregate(fns...)
}

func (meq *MetricEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range meq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, meq); err != nil {
				return err
			}
		}
	}
	for _, f := range meq.ctx.Fields {
		if !metricevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if meq.path != nil {
		prev, err := meq.path(ctx)
		if err != nil {
			return err
		}
		meq.sql = prev
	}
	return nil
}

func (meq *MetricEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MetricEvent, error) {
	var (
		nodes = []*MetricEvent{}
		_spec = meq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MetricEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MetricEvent{config: meq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, meq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (meq *MetricEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := meq.querySpec()
	_spec.Node.Columns = meq.ctx.Fields
	if len(meq.ctx.Fields) > 0 {
		_spec.Unique = meq.ctx.Unique != nil && *meq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, meq.driver, _spec)
}

func (meq *MetricEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(metricevent.Table, metricevent.Columns, sqlgraph.NewFieldSpec(metricevent.FieldID, field.TypeInt))
	_spec.From = meq.sql
	if unique := meq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if meq.path != nil {
		_spec.Unique = true
	}
	if fields := meq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, metricevent.FieldID)
		for i := range fields {
			if fields[i] != metricevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := meq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := meq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := meq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := meq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (meq *MetricEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(meq.driver.Dialect())
	t1 := builder.Table(metricevent.Table)
	columns := meq.ctx.Fields
	if len(columns) == 0 {
		columns = metricevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if meq.sql != nil {
		selector = meq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if meq.ctx.Unique != nil && *meq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range meq.predicates {
		p(selector)
	}
	for _, p := range meq.order {
		p(selector)
	}
	if offset := meq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := meq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MetricEventGroupBy is the group-by builder for MetricEvent entities.
type MetricEventGroupBy struct {
	selector
	build *MetricEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (megb *MetricEventGroupBy) Aggregate(fns ...AggregateFunc) *MetricEventGroupBy {
	megb.fns = append(megb.fns, fns...)
	return megb
}

// Scan applies the selector query and scans the result into the given value.
func (megb *MetricEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, megb.build.ctx, "GroupBy")
	if err := megb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MetricEventQuery, *MetricEventGroupBy](ctx, megb.build, megb, megb.build.inters, v)
}

func (megb *MetricEventGroupBy) sqlScan(ctx context.Context, root *MetricEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(megb.fns))
	for _, fn := range megb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*megb.flds)+len(megb.fns))
		for _, f := range *megb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*megb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := megb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MetricEventSelect is the builder for selecting fields of MetricEvent entities.
type MetricEventSelect struct {
	*MetricEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mes *MetricEventSelect) Aggregate(fns ...AggregateFunc) *MetricEventSelect {
	mes.fns = append(mes.fns, fns...)
	return mes
}

// Scan applies the selector query and scans the result into the given value.
func (mes *MetricEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mes.ctx, "Select")
	if err := mes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MetricEventQuery, *MetricEventSelect](ctx, mes.MetricEventQuery, mes, mes.inters, v)
}

func (mes *MetricEventSelect) sqlScan(ctx context.Context, root *MetricEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mes.fns))
	for _, fn := range mes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
